{
  "name": "ave-maria",
  "version": "1.0.0",
  "scripts": {
    "build": "build": "cd client && npm install && npm run build && cd ../server && npm install"
    "start": "node server/server.js"
  }
}


